# PR63 continuation — 2026-09-12

## Checkout and authority

Continue in `D:\OPTN wallet work\OPTNWallet-Desktop-worktrees\pr63-astra-resume-20260912`,
branch `agent/pr63-astra-resume-20260912`. Push with
`git push fork HEAD:agent/rpa-shared-vectors` (never force). The PR base is
OPTNLabs/OPTNWallet `dev`; the PR head lives on CyberAshven's fork.

The original `pr63-ui-runtime-parity` checkout is deliberately preserved.
Its committed Claude work has equivalent commits on the PR; do not replay it
again. Its unfinished Fusion app/transport changes were recovered, reviewed,
fixed and pushed. Its old dirty files/build directories are not a new task list.

The user authorizes fixes, focused/live Chipnet tests, small commits and pushes.
No mainnet spending. Never expose wallet secrets. Never dismiss or suppress
security alerts, exclude source to clear scans, or drop platform/quality gates.
Rust owns the application/runtime. GUI and CLI are independent interfaces;
CLI must not depend on Tauri or Leptos. Existing legacy interfaces remain for
comparison until Rust interface parity has actual running evidence.

## Pushed milestones and their limits

- `924adb72`: instruction/skill decision-boundary cleanup and truthful ledger.
  Global skill edits also exist outside Git; see the current local skills.
- `4bbb77e5`: shared Fusion session/transport recovery with cancellation,
  wallet/network lifecycle revocation and nonsigner restrictions.
- `61b11bc3`, `6e10e361`: restored vendored CodeQL coverage, regression against
  re-exclusion, and RustCrypto truncated-MAC verification. IV alerts 146/147
  are protocol-bound and remain open. See the dated security report.
- `36cd24e0`: live Chipnet HD/Tor test passed with two transactions and one
  UTXO; CLI and typed transport totals agreed. Encrypted checkpoint storage,
  a new runtime restoring stale state, live resync, outage retention and lock
  clearing passed. No GUI process, signing or paid Fusion round was tested.
- `b849ca94`, `f0270bb3`: shared reviewed Auto bootstrap in native GUI and CLI;
  exact/private policies and bootstrap bans retained. A fresh CLI reached
  Chipnet via shared-native-policy without persisting public defaults.
- `4f02f51c`, `218e9f6f`, `8f9e8d31`: canonical web quality gate restored;
  bounded Tor download retries; required Rust jobs trigger on every PR;
  locked Cargo resolution for all six manifests with an offline regression.
- `536aecf2`: upload the Rust Leptos Android debug APK that CI already builds,
  with merge/head revisions and SHA-256. The `4f3face1` APK was downloaded and
  exercised on an isolated Android 36 emulator; see the package evidence below.
- `f5539da5`: integrated concurrent agent commit `4f3face1`, preserving its
  Fusion timing and random test-fixture salt changes alongside our fix.
- `a9b5d6f4`: Rust connector CI uses the supported Node/Rust toolchains.
- `c687fd69`: shared manual-rescan intent persists before I/O, cancels old
  syncs, and survives private-wallet reopen. Checkpoint v3 retains pending
  intent and coverage while decoding v1/v2. CLI console, command and Tauri
  use the same runtime path; CLI workers share accepted headers with providers.
- `4aff74c7`: adds a Leptos macOS ARM64 preview DMG, retaining every legacy
  platform. Tor is bundled and verified; provenance/checksums and the complete
  artifact gate include the new package. The macOS CI build still must finish.

## Verified locally

399 app/runtime/transport tests passed after Fusion recovery. After source
parity: all 97 CLI tests, 10 runtime network-config tests and five native
network-store tests passed. Windows Tauri library compilation passed with the
pinned Tor bundle downloaded, checksum-verified and smoke-tested. Full
framework-neutral Clippy and the architecture boundary gate passed.

After the rescan change, all 211 runtime tests, 97 CLI tests, framework-neutral
Clippy and architecture validation passed. Tauri's Windows library compiled.
The connected private-wallet regression caught and fixed a real reopen bug:
`WalletSecurity::open` recaptured the checkpoint without copying its rescan
fields. Do not remove those assignments. A failed checkpoint save deliberately
invalidates active syncs; tests must retain that fail-closed behavior.
Live Chipnet rescan/restart passed at height 323122 in 77.33 seconds after one
source timeout on the first attempt. See `docs/chain-interop-evidence.md`.

Legacy dependency/license policies, lint, full TypeScript check and formatting
passed. Full coverage passed: 1,903 tests; statements 50.77%, branches 39.9%,
functions 51.94%, lines 52.45%. Those numbers describe that suite only. The
86 local formatting warnings were Windows line endings, with no tracked diff.

Use `CARGO_HOME=D:\Qubes\cargo`, `CARGO_INCREMENTAL=0`. Keep excluded Cargo
workspaces in distinct target directories: root workspace cache is currently
the old checkout's `target`, CLI uses this checkout's `target-cli`, and Tauri
uses `target-tauri`. Sharing root/CLI targets produced a duplicate-ECDSA type
cache failure; a fresh CLI target resolved it without source changes. Never
stage target directories or downloaded Tor resources.

## Current gaps and next connected work

PR63 and issues 71/75 are **not established end to end**. #84 is the closed
Vitest dependency PR, already incorporated into PR63, not a third wallet
architecture. Secondary Yarn/toolchain coordination is being checked separately.

1. Finish current CI failures and collect completed scans on the current PR
   revision. Cancelled/missing analyses are not an all-clear. Historical
   dismissals were left untouched. See `docs/security/2026-09-12-pr63-scan-coverage-and-fusion.md`.
   Current PR inventory: 14 open (GLib 113–124 and Fusion 146/147); all ten
   unresolved review threads are CodeQL. The GLib traces still conflate
   concrete generic types or omit C output initialization. No actionable
   current caller defect was established in that bounded review. Keep them
   visible; never restore the vendor exclusion or alter the Fusion wire IV
   to trick the analyzer. Source assessment is not scanner closure.
2. Manual rescan persistence is fixed and tested. Birthday provenance remains
   a component in `WalletRestoreState`; integrate actual create/import/open
   metadata next without conflating a manual height with a birth anchor.
   Packaged watch-only reopening is also missing: the tested APK opens the
   public Chipnet account but loses that session after force-stop/relaunch.
   Trace shared storage and wallet listing before adding persistence; do not
   solve this with renderer localStorage or Tauri-only business logic.
3. Native Tor discovery accepts an existing verified SOCKS daemon. Trace
   shared startup/progress before claiming fresh packaged GUI/CLI parity:
   process management still lives in `src-tauri/src/fusion/tor_manager.rs`,
   and mobile Leptos config omits bundled Tor resources. The new macOS package
   now restores canonical Tor resources; that is not shared Tor startup.
4. The shared Fusion model is not a running paid-round driver. Durable Auto
   preferences, runtime driver and independent GUI/CLI controls remain.
5. Finish SeedCash signing, real GUI Chipnet open/sync/restart, responsive UI
   against supplied references, addon/platform policy and APK/macOS testing.
   Do not substitute component or compile results for these workflows.

GitHub enforcement is still 11 required checks. The prepared 48-check set is
`docs/required-status-checks.json`; the connected account has write access but
no administration access, and GitHub rejected the ruleset update. An admin
must apply it to ruleset 20756244 while preserving current review protections.

## Packaged Android evidence and reproduction context

Artifact: `D:\OPTN wallet work\builds\2026-09-12\leptos-4f3face1\optn-leptos-android-aarch64-debug.apk`.
CI run 34690363927; artifact 10297122886. PR head `4f3face1`, compiled merge
`8efa221a99b927a6c6697c566fce030ca5d4de29`. SHA-256:
`6e971b8f3b9e71d170001e10c50efc0c5e55e20fb08abf81d89706241a0139fd`.
This APK predates `c687fd69`; do not call it the rescan-fix build.

The isolated AVD is `C:\tmp\optn-avd-20260912\optn-pr63.avd`, serial
`emulator-5582`, Android 36 Google APIs x86_64 with arm64-v8a translation.
SDK: `D:\OPTN wallet work\.android-build-tools\sdk`. It is offline (airplane
mode and Wi-Fi disabled). No user wallet directory was touched. The account
is the published BIP39 test fixture, derived by the built Rust core for
`m/44'/1'/0'`, and only its public xpub was supplied to the app.

Install, cold launch, landing, Chipnet selection, xpub validation and opening
the watch-only home passed. Balance correctly stayed unknown offline;
hardware wallet controls correctly reported desktop-only. Force-stop/relaunch
returned to landing. No signing, SeedCash import, live GUI sync, camera or
physical-device verification occurred. Screenshots, DOM helper and emulator
logs are in `C:\tmp\optn-avd-20260912`; they are evidence, not app source.

For UI continuation, follow `.claude/skills/ui-ux-product/SKILL.md` and the
reference images under `docs/ui-overhaul/`. Preserve the existing design and
responsive behavior; backend fixes are not permission to redesign screens.
