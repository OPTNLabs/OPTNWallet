# PR #63 UI/runtime handoff — 2026-09-07

## Goal

Finish Issue #71's Rust UI migration and Issue #75's native runtime path without
claiming a feature is complete until its renderer, transport, runtime, and real
target evidence agree. PR #63 at `d9deb7b3` was green when checked; this isolated branch is
`agent/pr63-ui-runtime-parity` for the next implementation slice.

## Do not lose these boundaries

### Completion-claim reconciliation (current source, 2026-09-07)

For newer runtime publication, CLI, live Chipnet, and TLS evidence, read
[the 2026-09-08 continuation](HANDOFF-2026-09-08.md). Later entries supersede
historical test counts and the earlier absence of live network evidence below.

This section supersedes any earlier wording that suggests full implementation.
The goal remains all of #71/#75 on PR #63, preserving existing functionality,
security/build gates, and platform deliverables. No architecture restart.

| Requirement | Current evidence | Remaining before completion |
| --- | --- | --- |
| Shared backend flow | Desktop and CLI use `optn-chain-native`; CLI status/select/ping/tx/broadcast/balance/utxos exercise shared policy | Account history/discovery/send paths still need migration; native GUI host does not mount `ProgressiveSyncWorker` or broadcast lifecycle |
| Source-policy persistence | Shared bounded/atomic writer, cross-process lock, exact CLI selection; host loop reload and cancellation tests pass | Rich source/pool editing and faithful GUI policy display; broader operation parity |
| Verified headers | SHV/MMR batch and worker tests pass, including tip/proof checks | Network-bound trusted checkpoint loading, consensus rules for selected chain/height, real adapter/reorg integration |
| Persistence and wallet state | Checkpoint codec exists; search finds no host caller of `encode_checkpoint_json`/`from_checkpoint_json` | Authenticated store lifecycle, verified transaction evidence, durable authoritative coins/history projection |
| Independent interfaces | CLI compiles without Tauri/Leptos and uses shared Rust policy; GUI renderer uses typed transport | End-to-end GUI/CLI use cases and consistent feature/platform gates |
| UI and platform verification | Local UI changes exist; old d9 commit has successful remote checks | Reference-image fidelity, simultaneous legacy/Rust comparison, device/emulator tests, new artifacts for the final changes |
| Security and CI | CodeQL was verified green on d9; local security fixes have focused tests | Current bot findings, all preserved gates, required-check administration, and remote validation of final commits |

Dependency order is shared backend flow -> persistence/authoritative wallet state
-> independent interface wiring -> UI fidelity -> platform verification.
Do not advance by calling component tests end-to-end completion. Close connected
milestones with host-level evidence and retain explicit remaining items.

The latest connected source-policy test runs a host loop, edits the shared file,
observes reload without restart, proves old service catalog revocation, handles
corruption/recovery, and cancels a real loopback socket probe when policy changes.
Four Tauri chain-runtime tests pass using the documented test-only resource
override. This is offline host integration, not a Chipnet wallet test. Runtime
polls external files every two seconds; application-state changes use a watch
channel. The Rust 1.98 incremental compiler panicked once; setting
`CARGO_INCREMENTAL=0` completed compilation and tests without deleting work.
Strict Clippy for the complete Tauri library also passes after moving a
test-only selection-plan import into the test module.

Backend transaction boundary follow-up: `ChainService::execute_on_route` now
requires lookup response identity and double-SHA256(raw) to match the requested
txid before marking a provider response successful. The broadcast coordinator
regression proves substituted raw bytes cannot promote an uncertain broadcast
to observed. A loopback Electrum server test crosses real negotiation, adapter,
and shared service for successful lookup and substitution rejection; its opaque
byte fixtures do not claim BCH transaction validity.

The existing core parser used by RPA and PSBT now requires the final locktime,
rejects trailing bytes, and enforces canonical CompactSize. A regression checks
all truncation points of its fixture. All 290 core, 66 runtime, 5 Electrum adapter,
and 114 CLI tests pass; strict core/runtime/Electrum Clippy passes. These close
the tested format/identity boundaries, not transaction inclusion, wallet projection,
or the overall backend milestone. No new live-wallet or platform evidence exists.

Wallet refresh follow-up: incremental scans now require a matching canonical
interest baseline with no height gap. They retain earlier history and pending
transactions rather than replacing the complete snapshot with a scan delta.
Omission from a block scan is not mempool eviction evidence. Failed, incomplete,
unavailable-route, and weaker-evidence refreshes mark retained state stale;
weaker evidence cannot replace a stronger snapshot by reporting another tip.
Electrum rejects unsupported outpoint/RPA scopes, BIP37 rejects RPA and scripts
without bloom items, and Neutrino rejects RPA/empty scripts rather than silently
scanning only a supported subset. The Electrum preflight test uses the real
loopback adapter harness and proves unsupported scopes do not open sessions.

Validation: 67 runtime, 6 BIP37, 5 Electrum, and 7 Neutrino tests pass; strict
Clippy passes for all four crates/all targets. These are offline component and
adapter tests. Full reorg rollback, per-transaction evidence reconciliation,
authenticated wallet-scoped persistence, and production worker mounting remain
unfinished. Aggregate incremental evidence is now capped by the baseline's
evidence: a stronger suffix response cannot upgrade the unscanned prefix, even
when that prefix has no known transactions. A worker regression demonstrates
that only a full rescan can upgrade the entire scope. Existing adapters currently
report server assertions for wallet refresh. This conservative aggregate policy
does not implement per-transaction proof provenance.

Wallet-lifecycle follow-up: all five wallet-open actions now share one completion
path. It clears prior coins, pledges, stealth balance, spend plan, connect request,
revealed identity, and cached hardware account before publishing WalletOpened.
Previously opening a second wallet without locking retained the first wallet's
balances and pending request. All validation gates execute before this reset;
rejected onboarding leaves the old wallet intact. Existing device selection and
physical attachment are preserved. The shared application regression exercises
created/imported seed, watch-only, hardware, and multisig replacement.
All 121 application and 68 runtime tests pass. No GUI/CLI wallet E2E or new artifact
completion is claimed; the worker still needs production lifecycle mounting.

CLI transaction follow-up: `optn tx` with durable shared configuration now calls
the native composition and ChainService transaction lookup before legacy client
setup. Source/protocol/fallback selection stays intact, raw identity is checked
by the shared service, and an empty policy cannot fall back to a public server.
Explicit endpoint flags and absent configuration preserve the existing legacy
behavior. Native-policy `--verbose` decodes locally with the existing `decode`
formatter; it returns local inputs/outputs/token fields, not provider-specific
verbose metadata or unproven confirmations. Native lookup and probing share the
CLI timeout budget. A test crosses Clap parsing, CLI dispatch, shared config,
native stack, and a real loopback Electrum server for raw/verbose success and
substitution rejection. This is offline command integration, not live Chipnet
or wallet sync. All 115 CLI tests and strict all-target CLI Clippy pass.
The architecture gate passes, and the locked CLI dependency graph contains no
Tauri or Leptos packages. Balance/send/history remain legacy paths; wallet sync
mounting and authenticated wallet-state persistence are still outstanding.

CLI local-operation follow-up: legacy client resolution is lazy and cached once
per command. Local inspect/decode/skills commands work with an empty advanced
policy or corrupt network file; network operations still reject those settings.
The regression exercises actual CLI parsing and dispatch without reading wallet
secrets or opening sockets. This avoids the previous global Electrum setup
blocking otherwise offline operations and preserves one endpoint during a scan.

Native Electrum now requires a host-selected expected genesis hash in its config.
Both initial probing and every reconnect validate `server.features.genesis_hash`
before any wallet-specific query. Native composition reuses the existing chain
genesis mapping; the loopback adapter regression changes/omits the identity on
reconnect and confirms no transaction query is sent on either connection.
Genesis identity is an advertised mismatch guard, not proof of consensus or
fork identity: BCH/BTC and Chipnet/testnet4 distinctions still need trusted
post-fork checkpoints. Existing peers without the required identity now fail
closed. Five Electrum and five native-composition tests plus strict all-target
Clippy pass. CLI's shared-policy loopback fixture now advertises Chipnet genesis.
All 116 CLI tests and strict all-target CLI Clippy pass after these changes;
the architecture boundary gate also passes.

CLI broadcast follow-up: `optn broadcast` with a shared policy now uses the native
stack and BroadcastCoordinator. It reports submitted/uncertain/rejected/unavailable
with the locally computed txid; submitted does not claim independent observation.
Probe timeout is unavailable; timeout after entering submission stays uncertain.
Uncertain/unavailable output exits 3 and deterministic rejection exits 5; read
policy still refuses before network access. Explicit endpoint overrides and
absent config keep the older CLI behavior. Send/token-send and other internally
constructed broadcasts still require migration; this does not claim their parity.
The existing CLI loopback test now exercises accepted broadcast and lost response,
plus unavailable empty policy. A subprocess regression verifies JSON/exit status
and read-policy refusal. Its initial config filename typo was fixed after the
failure; the fixture was byte `00`, never a spendable transaction or wallet key.
116 existing CLI tests plus the new subprocess test pass, as does strict CLI
all-target Clippy. No actual Chipnet broadcast, wallet E2E, or new artifact proof.

Shared transaction foundation: moved the existing CLI `tx.rs` and `token.rs`
implementations into `optn-core`; CLI modules now only re-export them. This makes
the existing CashTokens-aware decoder, transaction construction, BCH sighash,
and P2PKH signer available to the runtime without a CLI or UI dependency. No new
dependency or alternate implementation was added. The 19 existing module tests
moved with their code: counts are now 309 core and 98 CLI, not deleted coverage.

The shared decoder rejects trailing bytes, noncanonical CompactSize, malformed
token prefixes (previously silently treated as ordinary scripts), and lengths
that overflow the platform. Token commitment slicing uses checked conversion and
bounds. Existing truncation tests now assert errors; regressions exercise length
overflow and malformed tokens. Core/CLI suites and strict all-target Clippy pass;
the core also checks for `wasm32-unknown-unknown` and the architecture gate passes.
The final commitment-bound change passed its nine focused token tests, core
Clippy, and WASM check. These are format and shared-code evidence, not full BCH
consensus validation, WASM execution, or wallet output projection. Projection,
production sync lifecycle, and authenticated wallet persistence remain next.

Wallet projection milestone: `optn-core::tx::unspent_outputs` derives matching
outputs from raw transactions, removes spends independently of response order,
deduplicates identical observations, retains CashTokens metadata, and rejects
conflicting spends pending reconciliation. It is output discovery, not proof of
inclusion, completeness, maturity, or spendability. `WalletNetworkSnapshot`
derives confirmed balance and pending delta only for a requested script scope,
checking raw identity, reported height bounds, and arithmetic range first.
The regression includes a pending spend producing a negative unconfirmed delta.

`optn balance` with shared policy now executes native composition -> ChainService
-> ProgressiveSyncWorker -> shared output projection instead of legacy balance
RPC. The CLI loopback test covers that connected path and checks the response
remains labelled ServerAssertion. Empty/invalid policies still refuse fallback;
explicit overrides and absent config retain old behavior. This is a fresh,
single-address observation. P2P routes still need host-supplied trusted checkpoint
initialization before this worker can sync; they fail closed rather than downgrade
to Electrum. Full account discovery, persistent/reorg-aware wallet lifecycle, UI
publication, and GUI/CLI operation parity remain incomplete. All 310 core, 69
runtime, and 98 CLI tests pass, as do strict Clippy, core WASM compilation, and
the architecture gate. No live-wallet or platform-artifact completion is claimed.

UTXO parity follow-up: native-policy `optn balance` and `optn utxos` now share one
address refresh path and the runtime's checked script projection. UTXO output
includes locally identified txid/vout/value, asserted height, and preserved token
category/amount/NFT data. It does not claim maturity or authorization to spend.
Scope validation also rejects one transaction appearing with conflicting heights,
so confirmed and pending views cannot choose inconsistent interpretations.
The CLI loopback fixture now carries a token output and checks that balances and
UTXO totals agree and that the token amount survives the complete command path.
Single-address query integration does not replace full account discovery or
persistent wallet sync; those and trusted P2P initialization remain outstanding.
Validation for this follow-up: 69 runtime and 98 CLI tests pass; strict all-target
Clippy passes for both crates, and the architecture gate passes.

Token-custody model follow-up: shared `Coin` now retains optional TokenData;
WireCoin carries it as an optional field, preserving legacy plain-coin decoding.
Core ordinary BCH selection, explicit coin control, and Fusion eligibility all
exclude token-bearing coins independently of user freeze state. Removing a user
freeze cannot lift token custody protection. Protected BCH collateral remains in
reserved totals so it does not disappear from the portfolio. Explicit ordinary
send returns a token-aware-transfer error. GUI coin rows label token custody and
disable the misleading unfreeze action; this is data correctness, not UI-overhaul
completion or token-transfer UI implementation. Transport and shared spend tests
exercise metadata round-trip, unfreeze, automatic/manual selection, and Fusion
eligibility. 311 core, 121 app, 69 runtime, and 9 transport tests pass. Live sync
has not yet projected coins into the GUI host; this model boundary precedes it.
Strict all-target Clippy passes for the four shared crates. Leptos with the Tauri
transport compiles for WASM without warnings after removing an unused import;
the architecture gate passes. No rendered/device verification was performed.

Coin-state replacement component: `CoinSet::replace_chain_outputs` builds a
candidate replacement and publishes only after duplicate-outpoint validation.
For surviving outpoints it preserves local labels, holds, and Fusion depth;
observed value/address/token data come from the new projection. It discards
peer-supplied annotations for new outpoints. The regression checks removal of
spent outputs and rollback on duplicates. Eleven focused coin tests, strict
core all-target Clippy, and the architecture gate pass. This helper is not yet
mounted by the host; completeness and wallet/network binding are explicit caller
preconditions. Do not call it with a single-address subset of a larger wallet.
Before mounting projection, resolve zero-satoshi/token output representation:
the current Coin constructor rejects zero value, while raw output discovery
retains it. Do not silently drop token-bearing outputs or let one such output
turn a complete wallet refresh into a permanent failure.

- `optn-core -> optn-app -> optn-runtime -> optn-transport -> optn-ui` stays
  one-way. Renderers dispatch typed `AppAction`s and read `AppState`; they do
  not own wallet balances, history, signing, network policy, or platform rules.
- Preserve React/Capacitor until a matching Rust path has real-target evidence.
  The existing watch-only SeedCash flow is still the behavioral reference.
- The browser extension is `AppSurface::Extension`: a constrained read-only
  viewer with Home, Assets, Receive, and History. It must remain unable to
  prepare, sign, or broadcast a spend until a separately designed secure
  lifecycle exists. Do not give it desktop-native USB, Tor, clipboard, camera,
  or background-service powers by accident.
- Chipnet only for any live check. Never read, print, or use a recovery phrase,
  private key, or funded-wallet material from files or the environment.
- Do not present native chain sync, SHV/MMR verification, Tor-routed transport,
  hardware signing, PSBT signing, SeedCash round-trip, or CashFusion as live
  merely because a screen/model exists. Trace the host call and runtime first.

## Visual source of truth

Use `docs/ui-overhaul/` exactly, especially `A_home_single.png`,
`D_actions_collapsed.png`, `L_send_bch.png`, and `N_receive.png`. Issue #71
explicitly forbids inventing a second visual language for the Rust renderer.

Desktop and mobile share the same state/actions/feature rules. They use
different shell composition:

- mobile: fluid device width, safe-area bottom navigation, 44px targets;
- desktop: a normal resizable/maximizable window, rail, wider content grid,
  split portfolio/activity panels, and no stretched mobile card.

Do not add a second desktop/mobile/extension flag system. The existing shared model is:

`FeatureFlags` decides product permission, `transport_support(surface)` describes
physical transports, `hardware_vendors_for(surface)` intersects transport with
an integration declaration, and the renderer reads that result. CashFusion is
desktop-only because it needs its long-lived service. Hardware may be enabled
on mobile only when a real host provider exists; a grey state must explain that
the integration is absent, not falsely claim that a phone cannot use hardware.

All new work is Rust only: Rust core, Rust runtime, Rust transport, Rust-to-WASM,
and native Tauri providers. There is no new TypeScript, handwritten JavaScript,
browser QR/camera/clipboard helper, or duplicate front-end store. The Tauri
transport previously had a handwritten `inline_js` invoke shim; this branch
replaces it with Rust `js_sys` access to the existing Tauri global.

## Frontend quality guardrail

Treat the next backend-focused agent as a reviewer/implementer for runtime,
typed transport, and tests, not as visual-design owner. Any UI/CSS/markup change
must be compared against the canonical images on both a mobile-sized viewport
and a resizable desktop viewport before it is accepted. Do not replace the
reference screens with generic panels, fake QR codes, hard-coded balances, or
static success states.

## Changes started in this worktree

- Desktop Tauri window now opens at `1280x820`, remains resizable/maximizable,
  and keeps a `720x560` usable minimum. This removes the phone-shaped desktop
  default without affecting shared app logic.
- `AppTransport` now has a typed `write_clipboard` capability. The Tauri WASM
  adapter calls the existing `clipboard_write_text` Rust command; it does not
  use `navigator.clipboard` or a new JS bridge.
- `optn-ui/src/qr.rs` adds a local Rust QR SVG encoder for receive addresses.
  It contains one unit test and does not call a remote QR service.
- `ReceivePage` now renders the local QR, offers typed Tauri copy with an
  explicit unsupported/error result, shows a disabled Share control, and marks
  token receiving as unavailable until the token projection is wired.
- `WalletHome` now has a `wallet-home` hook for the desktop/mobile canonical
  grid CSS still to be completed.
- Native chain transport now uses the shared `optn_core::tor` route decision:
  core-defined loopback is the only direct exception; selected remote Electrum,
  BIP37, and Neutrino routes use their existing Rust SOCKS adapters only after
  the default Tor proxy passes the SOCKS verification; remote full-node RPC and
  event adapters stay fail-closed because their current implementations cannot
  consume that route. `BchnRpc`/`BchnZmq` are names of the present adapters,
  not a BCHN-specific architecture requirement: the runtime model remains node
  neutral. Six focused chain-runtime tests cover the no-dial, Tor-route, and
  unsupported-adapter boundaries without touching a remote endpoint.
- The Tauri-free CLI now uses the same core route decision. It skips Tor probing
  for loopback endpoints, refuses remote plaintext Electrum at construction,
  and otherwise opens each remote Electrum connection only through a freshly
  isolated Rust SOCKS route. The local SOCKS capability probe remains shell I/O;
  the policy itself remains in `optn-core`. `tokio-socks` is pinned in the CLI's
  own lockfile. The CLI test suite passed with 110 tests, including the no-direct
  fallback and fake-proxy guards.

The QR dependency is now pinned in `Cargo.lock`. Re-run the WASM and Tauri
checks listed below with a writable local `CARGO_TARGET_DIR`; this machine can
inherit an inaccessible `D:\Qubes\build\debug` target directory.

## Next implementation order

1. **Rust first:** trace and complete the native runtime path before more UI
   polish. `NativeChainRuntime` currently creates a `ChainService` but does not
   project a verified refresh into authoritative `AppState`. It now resolves
   the durable `UserNetworkOverlay` through the same Rust resolver as CLI;
   invalid configuration registers no providers. Continue with explicit transport policy, verified evidence,
   and a typed projection before a renderer says that it is synced.
   Do not remove the remote deny gate to make a connection work. The existing
   Rust SOCKS adapters are now wired for Electrum/BIP37/Neutrino and preserve
   proxy-side hostname resolution. The desktop and CLI currently use the same
   core decision with shell-local default 127.0.0.1:9050/9150 probes; move that
   route selection into a typed durable transport policy before adding custom
   proxies or mobile-managed configuration. Current full-node RPC/event adapters remain
   unavailable remotely until each has a verified proxy-capable implementation;
   do not treat the `Bchn*` adapter names as a node-brand architecture model.
2. Verify the hardware backend against the shared capability model. Native
   Ledger/Trezor command modules exist, but a UI/model flag is not evidence of
   a complete device account/signing lifecycle. Extension and mobile remain
   disabled until their own host provider is wired and tested.
3. Complete canonical CSS/markup for `WalletChrome`, Home, Actions, Receive,
   and Send from the reference screenshots only after the typed state it shows
   is real. Use `.shell-desktop` and `.shell-mobile`, not an arbitrary shared
   breakpoint that stretches mobile.
4. Make basic Actions buttons dispatch the existing Send/Receive routes. Show
   unavailable Swap/Connect/Scan/CashFusion states as disabled with accurate
   reasons until their native route exists.
5. Preserve Send's integer-satoshi parser until a tested exact BCH-decimal
   parser exists in shared Rust. Never use float conversion for money.
6. Trace legacy SeedCash Prepare -> Sign -> Broadcast behavior into typed Rust
   transport/runtime before removing React: animated `crypto-psbt` frames,
   density, fullscreen, bad-frame accumulation, signed-PSBT import, validation,
   and policy-routed broadcast all need real implementations.
7. Treat Issue #75 separately from visual polish: native runtime currently does
   not project provider events into wallet state or prove the full SHV/MMR/Tor
   transport path. Do not mark it end-to-end before that bridge exists.

## Required validation after code changes

```powershell
cargo fmt --all -- --check
cargo test -p optn-ui
cargo check -p optn-ui --target wasm32-unknown-unknown
cargo check -p optn-ui --target wasm32-unknown-unknown --features tauri-transport
cd crates/optn-ui; trunk build --release --config Trunk.tauri.toml
npm run tauri:leptos:build
```

The CLI has its own lockfile because it is intentionally excluded from the
desktop workspace:

```powershell
$env:CARGO_TARGET_DIR = "$PWD/target-codex-cli"
cargo test --manifest-path crates/optn-cli/Cargo.toml --locked
```

For the focused native chain test in this incomplete checkout, use a test-only
Tauri config because `src-tauri/resources/tor/` is a release artifact and is
not present here:

```powershell
$env:CARGO_TARGET_DIR = "$PWD/target-codex-runtime"
$env:TAURI_CONFIG = '{"bundle":{"resources":[]}}'
cargo test --manifest-path src-tauri/Cargo.toml --lib chain_runtime --locked
```

That override is only valid for compiling/running the offline unit test. Never
use it for a package, preview artifact, or release validation: those must fail
when the bundled Tor resource is absent.

Then inspect the actual desktop window at normal, narrow, half-screen, and
maximized sizes. Mobile requires an emulator/device run before claiming parity.

## Shared native routing continuation, 2026-09-07

The uncommitted `optn-chain-native` crate extracts concrete adapter construction
from the desktop shell. Desktop and CLI `ping` now construct the same stack from
the full persisted catalog/policy. `network status` inspects that selection
without connecting. Explicit endpoint flags remain a one-command Electrum override.
Other legacy CLI wallet commands still use Electrum and reject policies they
cannot enforce. Command-based selection editing/navigation, verified wallet sync,
persisted SHV/MMR, and GUI/CLI operation parity remain unfinished.

Validation: architecture gate passed; 60 runtime tests, 5 native adapter tests,
and 111 CLI tests passed. CLI all-target Clippy with warnings denied passed.
No new packages, funded transactions, or end-to-end Chipnet results were produced.
These working-tree changes are not yet committed or pushed.

Follow-up: fixed the legacy CLI's empty-overlay fallback. A present shared
configuration with no Electrum route now refuses a public default; an absent
network configuration remains distinguishable. All 112 CLI tests passed,
including the new empty-selection regression. GitHub was queried on 2026-09-07:
PR #63 remains open at d9deb7b348a7af03204b82b57cf0d892b9ec6227; both CodeQL
checks report SUCCESS. Those remote checks do not cover this uncommitted work.

SHV/MMR prerequisite fix: `ShvMmrHeaderVerifier::extend` now commits a header
batch atomically. Previously an invalid later header left earlier headers from
the rejected batch in the trusted accumulator/cursor. A regression proves both
empty-state rollback and preservation of an accepted checkpoint. All 61 runtime
tests pass. This does not mount the verifier into the live sync worker; that
integration and trusted checkpoint persistence are still required.

Next sync integration slice: `ProgressiveSyncWorker::with_header_verifier`
accepts a seeded SHV/MMR verifier with explicit difficulty context. BIP37 and
Neutrino refresh now fail closed without one. Their header pass validates
response start/count, runs the verifier on every batch, and publishes a cloned
candidate only after a complete bounded pass. The verified accumulator determines
the next height; the legacy config hint cannot override it. All 63 runtime tests
and strict runtime Clippy pass, including missing-verifier P2P refusal tests.
Remaining: positive multi-batch adapter integration tests, host-side trusted
checkpoint loading bound to the selected network, durable state/reorg handling,
and verified transaction/wallet projection. Electrum's existing server-assertion
path is not upgraded to verified-header evidence by this change.

Offline integration follow-up: synthetic low-difficulty headers now exercise
both BIP37 and Neutrino routes through multiple verified batches, retry from the
verified cursor, bad-header rollback across batches, and height-gap rejection.
The worker also rejects P2P wallet payload/observation tips that differ from
the locally verified tip. No evidence strength is upgraded by this check:
matching a tip alone does not prove transaction inclusion or scan completeness.
All 64 runtime tests and strict runtime Clippy pass. These tests use an in-process
backend, not a live peer or a funded wallet. Real adapter integration, trusted
network-bound checkpoint persistence, and wallet projection remain outstanding.

Checkpoint resume prerequisite: core MMR now computes `proof_for_next_leaf`
from current peaks, with at most 64 proof hashes and no historical leaves.
`ShvMmrHeaderVerifier` retains the verified tip header/proof atomically alongside
its accumulator and exposes `tip_checkpoint_proof`. The existing checkpoint
constructor can restore that material against a separately trusted root.
Tests cover 257 consecutive MMR sizes, corrupted proofs, and restoring the tip
after multi-batch worker verification. Five focused core MMR tests and all 64
runtime tests pass; strict core/runtime Clippy passes. No disk store is mounted
yet: authenticate the stored root and bind it to the network before treating a
persisted proof as trusted. Difficulty configuration must come from the host's
network policy, never from the untrusted checkpoint record.

Shared checkpoint codec: `encode_checkpoint_json` and `from_checkpoint_json`
now provide a bounded version-1 public record containing network, height,
commitment, tip header, and proof. Restore requires a separate trusted
`HeaderCheckpoint`, checks network/height/root before proof verification,
rejects unknown schema/fields and records above 16 KiB, and never imports ASERT
rules from disk. All 65 runtime tests and strict runtime Clippy pass.
The existing `optn-platform::SecureStorage` contract and optional
`optn-platform-native::NativeSecureStorage` provider are available, but neither
host has a checkpoint storage lifecycle yet. Do not call the JSON record itself
authenticated or use its own commitment as the independent trust input.

CLI selection/persistence integration: native `NetworkConfigFile` now owns the
single bounded reader and atomic writer used by CLI and Tauri. Updates hold a
stable sidecar file lock across read/validate/write, report contention instead
of hanging, and reject invalid/oversized configuration before replacement.
Tauri's compatibility save now checks the existing overlay while holding that
same lock, preserving rich policy edits made by CLI.

`optn network select <source-id> --protocol electrum|bip37|neutrino|node-rpc|node-events`
persists an exact selection from the existing catalog with no public fallback.
Unknown/disabled/banned/protocol-incompatible sources are refused. The new
`OPTN_POLICY=configure` level admits settings edits without secret/sign/spend
commands; read-only policy still permits status but refuses selection changes.
CLI tests: 114 passed, including rejected edits preserving bytes, lock contention,
Clap/manifest classification and permission enforcement. Five Tauri network-config
tests pass under the documented test-only resource override. CLI/native strict
Clippy and architecture gate pass.

Outstanding for this path: CLI source creation/editing, grouped/pool policy
navigation, and notifying an already-running desktop host about external file
edits. The host currently reads the new selection on startup/reconfiguration;
do not claim live cross-process synchronization yet. These edits remain local
and uncommitted, alongside the earlier UI/runtime work.
