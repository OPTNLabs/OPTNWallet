export enum PREFIX {
  mainnet = 'bitcoincash',
  chipnet = 'bchtest',
  testnet = 'bchtest',
}

export enum COIN_TYPE {
  bitcoincash = 145,
  testnet = 1,
  legacy = 0
}

export enum Network {
  CHIPNET = 'chipnet',
  MAINNET = 'mainnet',
}

export const INTERVAL = 300 * 1000; // 1-minute interval

export const DUST = 546;
