name: PR63 Tauri Lock Refresh

on:
  push:
    branches:
      - agent/rpa-shared-vectors
    paths:
      - 'src-tauri/Cargo.toml'
      - '.github/workflows/pr63-tauri-lock-refresh.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pr63-tauri-lock-refresh
  cancel-in-progress: false

jobs:
  refresh:
    if: github.repository == 'CyberAshven/OPTNWallet'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: agent/rpa-shared-vectors
          persist-credentials: true

      - name: Install pinned Rust
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.98.0

      - name: Resolve exact Tauri dependency graph
        run: cargo metadata --manifest-path src-tauri/Cargo.toml --format-version 1 >/dev/null

      - name: Commit generated Tauri lockfile
        run: |
          set -euo pipefail
          git add src-tauri/Cargo.lock
          if git diff --cached --quiet; then
            echo 'Tauri Cargo.lock already current.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'build(tauri): refresh transport lockfile'
          git push origin HEAD:agent/rpa-shared-vectors
