[package]
name = "optn-app"
version = "1.7.3"
edition = "2021"
license = "GPL-3.0-only"
description = "Framework-neutral application state and use-case layer for OPTN Wallet."

[dependencies]
optn-core = { path = "../optn-core", version = "=1.7.3" }
# Capability ports only — no shell or framework comes in with this.
optn-platform = { path = "../optn-platform", version = "=1.7.3" }

[dev-dependencies]
proptest = "1.9.0"
